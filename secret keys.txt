⚠️  CREDENTIALS REMOVED — rotate any keys that were stored here.

Store secrets in .env.local (gitignored) or Vercel project environment variables only.
NEVER commit credentials to files tracked by git.
